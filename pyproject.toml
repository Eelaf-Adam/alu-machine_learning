[project]
name = "alu-machine-learning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Eelaf-Adam", email = "e.adam2alustudent.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
alu-machine-learning = "alu_machine_learning:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
